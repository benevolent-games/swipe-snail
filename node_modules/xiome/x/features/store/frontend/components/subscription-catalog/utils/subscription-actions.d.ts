import { makeStoreModel } from "../../../model/model.js";
import { SubscriptionTier } from "../../../../isomorphic/concepts.js";
import { ModalSystem } from "../../../../../../assembly/frontend/modal/types/modal-system.js";
import { TierButton } from "../../../views/tier/types.js";
export declare function preparePurchaseActions({ modals, button, tier, storeModel }: {
    button: TierButton;
    modals: ModalSystem;
    tier: SubscriptionTier;
    storeModel: ReturnType<typeof makeStoreModel>;
}): {
    upgradeOrDowngrade: () => Promise<void>;
    buySubscriptionWithCheckoutPopup: () => Promise<void>;
    buySubscriptionWithExistingPaymentMethod: () => Promise<void>;
};
