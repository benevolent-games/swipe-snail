export function getOngoingSubscriptions(storeModel) {
    var _a;
    return ((_a = storeModel
        .get
        .subscriptions
        .mySubscriptionDetails) !== null && _a !== void 0 ? _a : [])
        .map(subscription => {
        const plan = (storeModel
            .get
            .subscriptions
            .plans
            .find(p => p.planId === subscription.planId));
        const tier = (plan
            .tiers
            .find(t => t.tierId === subscription.tierId));
        return {
            plan,
            tier,
            subscription,
            pricing: subscription.pricing,
        };
    });
}
//# sourceMappingURL=get-ongoing-subscriptions.js.map